import { vi } from "vitest";

// وحدات التطبيق الخادمية تحمل `import "server-only"`، وهو حزمة تعمل داخل Next.js فقط.
// الاختبارات الوحدوية تعطّله لتستطيع استيراد الوحدة الخادمية واختبار منطقها النقي.
vi.mock("server-only", () => ({}));
