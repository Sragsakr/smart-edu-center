name: Set up project

description: Check out the repository and install its locked Node.js dependencies.

runs:
  using: composite
  steps:
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version-file: .nvmrc
        cache: npm
    - name: Install locked dependencies
      shell: bash
      run: npm ci
